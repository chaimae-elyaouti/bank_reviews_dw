name: 'transform_bank_reviews'
version: '1.0.0'
config-version: 2

profile: 'transform_bank_reviews'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  transform_bank_reviews:
    staging:
      +materialized: view
    marts:
      +materialized: table